<% provide(:title, 'All users') %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <div class="user_image">image</div>
      <%= link_to user.name, user, class: "user_name" %>
    </li>
  <% end %>
</ul>