<!--renderする時 locals: size, shop -->

<%= link_to shop_path(shop) do %>
  <% if shop.image.attached? %>
    <%= image_tag shop.display_image, :size => size, class:"shop_image" %>
  <% else %>
    <%= image_tag "default_shop.jpg", :size => size, class:"shop_image" %>
  <% end %>
<% end %>